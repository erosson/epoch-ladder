{
  "name": "epoch-rank",
  "description": "",
  "version": "1.0.0",
  "main": "src/index.js",
  "repository": "https://github.com/erosson/epoch-rank",
  "private": true,
  "scripts": {
    "start": "elm-app start",
    "build": "elm-app build",
    "lint": "elm-format --validate ./src",
    "test": "elm-app test",
    "start:no-debug": "bash -c \"ELM_DEBUGGER=false elm-app start\"",
    "serve": "serve -s build",
    "serve:build": "bash -c \"yarn build && yarn serve\""
  },
  "devDependencies": {
    "create-elm-app": "^5.22.0",
    "elm-format": "^0.8.4",
    "prettier": "^2.2.1",
    "serve": "^11.3.2"
  }
}
